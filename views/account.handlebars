<!DOCTYPE html>
<html lang="en">

<head>
  <title>Wildlife Observers</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src='https://api.mapbox.com/mapbox-gl-js/v1.12.0/mapbox-gl.js'></script>
  <link href='https://api.mapbox.com/mapbox-gl-js/v1.12.0/mapbox-gl.css' rel='stylesheet' />
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.7/lumen/bootstrap.min.css">
  <link href="stylesheets/style.account.css" rel="stylesheet">
</head>

<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <div class="row">
                    <div class="col-md-6 col-md-offset-3"></div>
                        <h2>About <span class="member-name"></span></h2>
                    </div>
                    <div class="col-md-1 col-md-offset-3">
                        <a class="navbar-brand" href="/logout">Logout</a>
                    </div>
                </div>
            </div>
        </div>
    </nav>
    <div class="conatiner">
        <div>
            <input type="" value="{{user.userName}}">
            <button>Change UserName</button>
            <input value="{{user.email}}">
            <button>Change Email</button>

        </div>
        <div class="previous-posts container">
            <h1>Previous Posts</h1>
            <ul class="user-posted-list">
            {{#each animal}}
                {{> animals/userAnimals }}
            {{/each}}
            </ul>
        </div>
    </div>
    




<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script type="text/javascript" src="js/account.js"></script>
</body>

</html>
